# Программный интерфейс: {{#unless isCommonModule}}{{type}}.{{/unless}}{{present}}

{{~#each methods}}

### `{{name}}({{#each parameters}}{{#if @index}}, {{/if}}{{name}}{{/each}})`

{{#if description}}{{description}}{{/if}}
    {{~#if parameters}}

| Имя | Тип | Описание |
|-----|-----|---------|
{{~#each parameters as | parameter|}}
    {{~#each parameter.types as | type typeIndex|}}
| {{#unless typeIndex}}`{{parameter.name}}`{{/unless}} | `{{type.name}}` | {{#single-line}}{{links type.description}}{{/single-line}} |
    {{~else}}
| {{name}} | | {{description}} |
    {{~/each}}
{{~/each}}
    {{~/if}}
{{~#if returnedValue}}

Возвращаемое значение

{{#if returnedValue.link}}
[{{returnedValue.link}}]({{returnedValue.link}})

    {{~else}}
`{{returnedValue.name}}` - {{returnedValue.description}}
    {{~/if}}
{{~/if}}
{{~/each}}
